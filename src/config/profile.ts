import { ProfileConfig } from "@/types";

export const profileConfig: ProfileConfig = {
  mainNav: [
    {
      title: "Products",
      href: "/products",
    },
    {
      title: "Profile",
      href: "/profile",
    },
  ],
};
